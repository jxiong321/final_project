[project]
name = "pipeline"
version = "0.1.0"
description = "Async data pipeline"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.13.5",
    "rich>=15.0.0",
    "typer>=0.25.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
